# AI 旅行规划 Agent 需求说明（确认稿 v0.8）

创建日期：2026-05-31
状态：已根据当前对话沉淀第一版确认需求；未明确部分统一标记为 `TBD`，等待后续确认。

## 1. 产品定位

面向自由行用户的 AI 旅行规划 Agent。产品目标不是只生成一段文本攻略，而是将用户的内容灵感、个人偏好、预算、时间、交通约束转化为可编辑、可比较、可执行的旅行方案。

核心价值包括：

- 将小红书、抖音、攻略截图、攻略文本、地图截图等内容转化为可执行 itinerary。
- 用地图、路线、POI、距离、费用和每日时间轴共同表达行程。
- 为用户提供多个可比较方案，并说明方案之间的取舍依据。
- 支持用户持续与 Agent 对话定制方案，并保存可复用的个人旅行设定。

## 2. 目标用户

当前已确认目标用户：

- 自由行用户。
- 第一版主要服务国内游用户。
- 需要从社交内容、攻略、截图中获得灵感并落地成行程的人。
- 需要在预算、时间、交通方式、景点预约、路线强度之间做决策的人。
- 首批用户不限定单一人群，覆盖学生、情侣、家庭亲子、朋友结伴、摄影用户和泛自由行用户。

## 3. 核心功能范围

### 3.1 内容到行程

用户可以上传或输入以下内容：

- 小红书截图。
- 抖音截图。
- 攻略截图。
- 攻略文本。
- 地图截图。
- 小红书或抖音链接。
- 单张风景照片。
- 多张图片，系统需要自动聚合成一个旅行灵感集合。

系统需要从上述内容中提取并结构化以下信息：

- 地点与 POI。
- 旅行风格，例如拍照打卡、低预算、轻松不赶路等。
- 预算线索。
- 路线顺序或空间关系。
- 用户可能想复刻的内容灵感。
- 对单张风景照片，识别或推断相同景点、相似景点或同风格景点推荐。

系统需要将提取结果转化为“同款但更 DIY”的行程：

- 生成可执行 itinerary，而不是只输出攻略文本。
- 支持用户调整地点、天数、预算、交通方式和节奏。
- 支持根据用户修改重新计算或重排局部行程。

系统需要查询或估算行程执行条件：

- 不同交通方式下的可行性，例如飞机、火车、高铁、自驾等。
- 交通是否有票或是否可达。
- 景点是否需要预约。
- 景点门票、交通票价、路费等对应开销。

未确认边界：

- 第一版 MVP 只做票务查询，不做下单、支付、出票闭环。
- Demo 阶段票务查询必须真实联网查询。
- 如果部分实时数据不可得，可以使用“估算费用 + 外部购票入口 + 数据来源/更新时间/置信度”作为辅助兜底，但不能替代真实联网查询这一主路径。
- 第一版票务查询覆盖高铁/火车票、机票、景点门票。
- 如果景区存在提前预约要求，需要提示景区预约名额或预约入口。
- 票务真实联网查询优先接入正式票务查询 API；如果正式 API 暂时无法接入，则通过第三方搜索或聚合入口完成查询。

### 3.2 空间规划

产品输出形态应包含地图与时间轴，不仅是文本。

主规划页面的已确认布局：

- 左侧：用户需求与 Agent 对话。
- 中间：3D 地图、POI、路线距离。
- 右侧：每日时间轴 itinerary。

中间地图区域需要支持：

- 展示关键路径点。
- 在关键路径点展示照片。
- 第一版地图候选点不使用 mock 数据；进入地图或切换城市后，地图默认不展示任何候选标点。
- 地点未完全确定时，不自动展示热门打卡点；只有用户搜索、点击地图地名或围绕某个已选地点发起附近搜索后，才展示真实地图服务返回的候选 POI。
- 展示路线距离。
- 与右侧时间轴联动，帮助用户理解每天的空间移动和时间安排。

地图工作台交互细化：

- 地图相关 POI、候选点、照片、地名缩略图和搜索结果必须来自真实地图服务结果；地图服务失败时直接展示失败原因，不展示 mock POI 或 mock 标点。
- 进入地图时，上方搜索区域默认分类状态为“全部”，但地图上不出现任何候选标点。
- 上方搜索栏快捷分类只保留“景点”“美食”“交通”。不再展示“体验”“购物”快捷按钮；用户仍可通过关键词搜索体验类内容。
- 用户输入关键词并搜索时，系统在当前城市或当前地图上下文中查询真实 POI，候选 POI 结果才出现在地图下方或浮层中。
- 当搜索关键词为空时，系统不移动地图中心、不新增候选标点，只保留当前地图视角。
- 用户点击地图中的地名或候选 POI 后，需要在对应标点上方展示该地点的缩略景点图。
- 缩略景点图可点击放大查看；放大查看在当前页面内完成，不跳转页面。
- 放大查看时，需要展示来源网址、地点介绍，并支持左右滑动查看该地点的其他照片。
- 用户拖动地图时，当前地点缩略景点图和放大查看层需要关闭。
- 当前地图右下方的独立缩略图抽屉需要移除，照片展示应绑定在被点击的地图标点附近。
- 用户点击某个地点后，缩略图上方还需要展示一个小搜索框，支持以该点坐标为中心搜索附近地点。
- 地图进入与地图拖动后的加载速度需要优化，避免进入地图或移动地图时阻塞主要交互；应优先复用已加载的地图实例、按需查询 POI，并避免空搜索触发无意义请求。

主页面三栏联动与可调整布局：

- 左侧 Agent 对话框可以点击收缩到左侧，也可以释放展开。
- 中间地图与右侧时间轴之间的宽度比例可以由用户手动拖动调整。
- 右侧时间轴可以点击收缩到右侧，也可以释放展开。
- 地图选择、Agent 对话和右侧时间轴安排需要共享当前规划上下文，包括当前城市、当前地图选择地点、当前时间轴选中段、候选 POI 和当前行程。
- Agent 对话需要能读取用户在地图中选择的地标和右侧时间轴当前安排，作为理解“这个地方”“这一段”“放到下午”等自然语言指令的上下文。

右侧时间轴优化需求：

- 右侧时间栏顶部的“行程总览 / 行程对比 / 费用明细”需要具备真实切换能力；当前阶段优先实现“行程总览”，其余页签可保留入口但必须清楚显示未接入状态或后续能力提示。
- 行程总览顶部只展示可编辑行程标题，例如“北京 2 日行程”；标题不再附带“可编辑”字样。
- 用户点击行程标题后，可以直接编辑旅行标题；编辑需要支持确认、取消和基础校验，例如标题不能为空、长度不可过长。
- 删除标题下方的小字描述，例如“轻松不赶路 / 历史路线 / 老北京风情”等模板说明文案。
- 标题旁的“优化路线”和“自动排期”按钮保留为后续 Agent 能力入口；当前阶段不要求接入左侧 Agent，也不要求实现真实优化或排期逻辑。
- 天气预警或景点风险提示保留为后续 Agent 能力入口；未来当 Agent 对话中识别出具体旅行日期后，系统需要联网查询天气，并结合景区公开信息与景点特征生成天气预警或景点风险提示。
- 行程总览顶部现有的全局“步行、预计耗时、预计花费”和“交通方式选择”模块需要移除，不再放在 Day 分组上方。
- 时间轴应按每日分组展示，每个 Day 是一个独立框体，例如“Day 1 历史中轴线与老北京风情”。
- 每个 Day 框体可以整体折叠和展开；折叠时仍应保留 Day 编号、当日标题和当日汇总信息。
- 每个 Day 框表头展示该日总步行距离、总预计耗时和总预计花费。
- Day 框内以时间线形式展示该日景点或活动；每个景点或活动是一个小框。
- 每个已解析且非明确休息日的 Day 都必须保留用户要求的全部必去地点，并由服务端冻结不可向下缩减的当日完成目标；Day 1 的额外活动不能抵消 Day 2 的缺项。
- 标准或紧凑节奏下，如果完成用户必去地点后仍有明显空余时段且搜索/路线预算允许，服务端应在最终候选与路线分配前增加至多一个顺路、轻松、符合该方案主题的真实地图地点。该地点缺少真实 POI 或最终相邻路线证据时，方案必须继续显示为待补/不完整，不得生成占位地点，也不得把目标数降到当前已找到的数量。
- 每个景点或活动小框至少包含：可编辑时间点、景点/活动名称、Agent 生成的注意事项或风险提示、预计耗时、预计花费。
- 景点注意事项需要由 Agent 基于网络搜索、景区公开描述、票务/预约信息、天气和拥挤情况生成；当前没有接入真实能力时，可以展示结构化占位或“待 Agent 查询”提示，但字段需要预留。
- 每个时间点都允许用户修改；修改时需要做基础校验，例如时间格式合法、同一天内时间顺序不冲突、明显跨日或倒序时给出提示。
- 每个 Day 框底部提供“添加景点/活动”入口，用于向当天追加日程项。
- 所有 Day 框下方提供“增加日程安排”入口，用于新增 Day 2、Day 3 等后续天数。
- 右侧时间栏底部展示总计区域，统计所有 Day 的总费用、总时长和总步行距离。
- 总计数据需要根据用户当前所有日程安排动态统计；当用户修改时间、费用、活动或增删 Day 时同步更新。
- 当前阶段右侧时间栏中用户可直接修改的字段包括旅行标题、每日标题和每个景点/活动的时间点；其他字段优先由 Agent 生成。
- 右侧时间栏状态需要可序列化为 Agent 上下文，包含旅行标题、每日安排、每个活动时间点、POI、注意事项、预计耗时、预计花费、每日汇总和总计，以便后续用户与 Agent 对话时作为当前行程上下文传入。

路线规划页面需要支持：

- 在用户确定旅游景点后，保存不同路线规划。
- 对不同路线进行比较。
- 允许用户设置路线约束，例如时间、交通方式、路过景点。

未确认边界：

- 第一版希望使用 3D 地图作为核心空间规划界面。
- 第一版不要求真实三维地形或三维建筑，采用 3D 视角地图、立体路线和 POI 卡片即可。
- 地图服务商当前按高德地图方向接入，优先保证 Web 地图、POI 搜索、路线规划和照片信息可用。
- 地图服务商选型需要重点评估国内 POI 覆盖、路线规划能力、Web SDK、3D 视角能力、费用/免费额度、开发接入复杂度和许可限制。

### 3.3 可比较方案

Agent 不只给一个答案，而是输出多个可比较方案，并给出决策依据。

已确认的方案类型示例：

- 方案 A：低预算。
- 方案 B：拍照优先。
- 方案 C：轻松不赶路。
- 第一版固定 3 个核心方案模板：低预算、拍照优先、轻松不赶路。

每个方案需要说明：

- 路线安排。
- 时间安排。
- 预算或费用拆分。
- 交通方式。
- 景点预约或购票要求。
- 方案优点。
- 方案取舍或风险，例如赶路、费用高、预约难、天气依赖等。
- 预算作为软约束处理，系统给出用户预算左右的方案即可；超出预算时说明差额和原因，不要求强制重排。

用户可以通过对话继续定制个人方案：

- 用户可要求 Agent 按个人偏好修改方案。
- 用户可保存个人设定。
- 保存后的设定可在下次规划时继续使用。

个人旅行设定的第一版策略：

- 初始阶段由 Agent 基于与用户的沟通动态推断设定字段。
- Agent 在对话中提取偏好，并显示“偏好摘要卡片”供用户确认。
- 用户可以在偏好摘要卡片中新增、删除或修改偏好内容。
- Agent 需要理解用户自定义偏好含义，并提供已定义的相似标准字段供用户选择或合并。
- 可作为标准字段的方向包括预算上限、旅行节奏、交通偏好、住宿偏好、餐饮偏好、拍照偏好、亲子友好、无障碍需求等。
- 人数信息保存为长期偏好，但用户可以手动修改。
- 人数偏好需要记录同行人类型，例如成人、儿童、老人、宠物、行动不便者等。
- 第一版不做复杂字段类型配置，例如文本、单选、多选、预算数字、优先级滑杆等。

### 3.4 多人共同规划

第一阶段优先完成单人基础规划功能。多人共同规划、分享链接、评论或投票作为后续阶段能力。

后续进入多人共同规划 Demo 阶段时，需要实现真实多人在线协作，而不是只用静态页面模拟流程。

核心能力包括：

- 用户可以分享行程方案链接。
- 其他参与者可以查看方案。
- 参与者可以对景点、路线或方案进行评论。
- 参与者可以对候选方案、POI 或时间安排进行投票。
- Agent 可以根据评论和投票结果辅助生成折中方案。

## 4. 关键用户流程

### 4.1 从内容灵感生成行程

1. 用户上传小红书截图、抖音截图、攻略文本、地图截图、社交平台链接、单张风景照片或多张图片集合。
2. 系统识别内容中的地点、风格、预算、路线、城市和关键约束。
3. Agent 向用户确认识别结果和缺失信息。
4. 系统生成一个或多个初始 itinerary。
5. 用户在地图、时间轴和对话中调整方案。
6. 系统更新路线、费用、时间、交通和预约信息。
7. 用户保存最终方案。

### 4.2 从已选景点生成路线规划

1. 用户确认想去的景点或 POI。
2. 系统基于时间、交通方式和路过景点约束生成多条路线。
3. 用户比较路线距离、耗时、费用和游玩强度。
4. 用户选择或继续修改路线。
5. 系统保存路线规划版本。

### 4.3 保存个人旅行设定并复用

1. 用户通过对话表达偏好。
2. Agent 将偏好结构化为可保存设定，并生成偏好摘要卡片。
3. 用户在偏好摘要卡片中确认、修改、新增或删除偏好内容。
4. 下次生成行程时，系统自动应用该设定。

## 5. AI Agent 能力要求

### 5.1 多模态理解

Agent 需要理解截图、文本和地图类输入，并从中抽取可规划的信息。

能力包括：

- OCR 识别截图文本。
- 解析小红书或抖音链接中的可用文本、图片和地点线索。
- 当小红书或抖音链接解析受平台限制时，支持用户手动粘贴正文或上传截图作为兜底。
- 理解单张风景照片，推荐相同景点、相似景点或同风格景点。
- 理解多张图片集合，将其中的地点、风格、预算线索和路线线索聚合为一个旅行灵感集合。
- 识别地点名称、景点名称、商圈、城市、路线描述。
- 从攻略语气或标签中识别旅行风格。
- 从价格、天数、交通描述中提取预算和约束。
- 对不确定识别结果给出置信度，并向用户追问确认。

### 5.2 行程规划

Agent 需要将地点、时间、预算、交通和用户偏好组合成可执行计划。

能力包括：

- 日程拆分。
- POI 排序。
- 游玩时长估算。
- 交通方式选择。
- 路线距离和耗时估算。
- 费用拆分。
- 预约和购票要求提示。
- 多方案生成与比较。
- 天气信息参与规划，例如雨天备选方案、户外景点风险提示。
- 人数信息参与规划，例如多人出行时的交通、餐饮和节奏建议。
- 交通拥挤程度参与规划，例如高峰时段提醒、拥堵风险提示、路线替换建议。
- 交通拥挤程度既需要真实数据源，也允许基于地图路线耗时、时间段和公开信息做风险估计。
- 交通拥挤提示需要影响景点游玩顺序和出发时间推荐，并向用户说明调整原因。
- 天气信息需要同时支持逐小时天气、每日天气摘要和风险提示。
- 距离旅行开始时间越近，如果出现坏天气或天气恶化趋势，需要提高提醒频次；如果天气为理想状态，则降低提醒频次。
- 坏天气不提供用户自定义阈值配置，由 Agent 基于对话中识别出的旅游目的判断；只要会影响本次旅行目的，即视为坏天气。
- 旅游当天需要使用邮件提醒天气风险。

### 5.3 对话式编辑

Agent 需要支持用户通过自然语言修改方案。

示例能力：

- “把预算降到 3000 以内。”
- “不要太赶，下午多留休息时间。”
- “优先拍照打卡。”
- “这天不要坐高铁，改成自驾。”
- “必须路过这个景点。”
- “删掉这个景点，换成附近更小众的。”

### 5.4 决策解释

Agent 输出方案时需要解释依据，而不是只给结论。

解释维度包括：

- 为什么这样排序 POI。
- 为什么推荐某种交通方式。
- 为什么某个方案更便宜、更适合拍照或更轻松。
- 哪些信息来自实时查询、哪些是估算。
- 哪些风险需要用户提前确认。

## 6. 数据与集成需求

当前已确认需要的数据类型：

- POI 数据。
- 地图与路线数据。
- 交通方式与耗时数据。
- 票务或预约要求数据。
- 天气数据。
- 交通拥挤程度数据。
- 景点门票数据。
- 交通费用或路费数据。
- 用户上传内容。
- 用户偏好与保存设定。
- 公开搜索结果。
- 地图服务结果。
- 协作评论、投票和分享链接数据，作为后续多人协作阶段数据。
- 真实数据库中的行程、偏好、字段配置、上传材料和查询结果记录。
- 上传图片的结构化识别结果、缩略图和可选长期原图。

待确认集成：

- `TBD`：地图服务商，按国内 POI、路线规划、3D 视角、Web SDK、费用/免费额度和开发效率综合选择。
- `TBD`：机票查询服务，优先正式 API，不可用时使用第三方搜索或聚合入口。
- `TBD`：火车/高铁票查询服务，优先正式 API，不可用时使用第三方搜索或聚合入口。
- `TBD`：景区门票与预约服务，优先正式 API，不可用时使用第三方搜索或聚合入口。
- `TBD`：酒店、餐厅或本地生活服务是否纳入范围。
- `TBD`：天气数据服务。
- `TBD`：交通拥挤程度数据来源。

当前已确认的技术与成本约束：

- LLM 服务商使用 DeepSeek。
- DeepSeek 如具备可用多模态能力则优先用于多模态理解。
- 如果 DeepSeek 多模态能力不足，允许组合其他免费或低成本视觉模型。
- 前端、后端、数据库暂无技术偏好，后续按个人 Demo 成本和开发效率建议技术栈。
- 第一阶段 Demo 需要使用真实数据库保存数据，不使用纯浏览器本地存储或临时会话作为主存储。
- AI Demo 阶段使用 SQLite；准备上线 MVP 时切换 PostgreSQL。
- 第一版使用单机默认用户保存到数据库，不要求注册或登录。
- 上传图片和截图默认只保存结构化结果和缩略图，原图短期缓存后自动删除。
- 只有当用户明确选择“保存灵感原图”时，才长期保存原图。
- 原图短期缓存清理策略确定为手动清理。
- 票务查询结果不保存长期历史快照，每次打开行程时重新查询最新结果。
- 当前无固定开发周期。
- 当前无团队规模规划，默认按个人探索项目推进。
- 初期探索优先使用免费或低成本方案。
- DeepSeek、地图、票务、天气、搜索需要预留可替换 provider 接口。
- 第一版只实现一个默认 provider 和一个 mock provider。

## 7. 产品形态

第一版首发形态已确认为 Web。

当前已确认：

- 以浏览器访问为主。
- 第一版功能集中在一个主页面，不拆成多个业务页面。
- 页面需要承载 Agent 对话、3D 地图、POI、路线距离、每日时间轴等核心区域。
- 第一版更偏 AI Demo 验证效果，而不是完整可上线产品 MVP。
- 第一版不需要移动端浏览器适配。
- 第一版先个人使用，后续再开发登录账户和跨设备同步。
- 第一阶段优先完成单人基础功能；多人真实在线协作进入后续阶段。
- 第一版不需要导出 PDF、图片、日历或 Markdown。
- 第一版重点样例城市为北京、上海、广州、深圳。
- 北京、上海、广州、深圳需要覆盖同等完整的 POI、票务、天气和交通拥挤提示能力。
- 第一版同时提供显式城市切换/城市选择入口，并支持从用户输入和上传内容中自动识别城市。
- Demo 不预置固定高质量样例数据作为主要体验路径，优先依赖用户输入、公开搜索结果、地图服务结果和真实查询结果。
- 地图相关体验彻底取消 mock 数据；地图服务不可用时展示错误原因，不展示 mock 标点、mock 候选 POI 或 mock 照片。
- 邮件提醒在 Demo 阶段先模拟发送状态，不真实发送邮件。
- 第一版在创建提醒时临时填写邮箱，并只绑定当次灵感或当次行程提醒。

## 8. 核心数据对象

建议后续围绕以下对象建模：

- User：用户。
- PreferenceProfile：用户旅行设定。
- PreferenceSummaryCard：偏好摘要卡片。
- TripRequest：一次旅行规划请求。
- SourceMaterial：用户上传的截图、文本或地图内容。
- POI：地点或景点。
- ItineraryPlan：行程方案。
- ItineraryDay：某一天的行程。
- ItinerarySegment：某一段活动或交通。
- RouteOption：路线方案。
- CostItem：费用项。
- ReservationRequirement：预约或购票要求。
- WeatherSignal：天气信号。
- TrafficCrowdingSignal：交通拥挤程度信号。
- PlanComparison：多方案比较结果。
- CollaborationSpace：多人共同规划空间，后续阶段使用。
- ShareLink：分享链接，后续阶段使用。
- Comment：评论，后续阶段使用。
- Vote：投票，后续阶段使用。

## 9. 验收标准草案

以下为第一版验收标准，具体数值待后续确认：

- 用户可以上传至少一种内容源，并生成结构化识别结果。
- 用户可以上传多张图片，并由系统聚合为一个旅行灵感集合。
- 用户可以看到识别出的地点、预算线索、风格和路线线索。
- 系统可以基于同一需求生成至少 3 个差异化方案。
- 每个方案包含每日时间轴、POI、交通方式、费用和预约提示。
- 地图视图可以展示 POI、关键路线点、路线距离，并与时间轴对应。
- 第一版地图视图以 3D 地图为目标形态。
- 用户可以通过对话修改行程，修改后方案能够更新。
- 用户可以保存一个最终行程方案。
- 用户可以通过偏好摘要卡片确认个人旅行设定，并在后续规划中复用。
- 用户可以在偏好摘要卡片中新增、删除或修改偏好内容。
- 票务查询需要真实联网查询高铁/火车票、机票、景点门票，并在景区需要提前预约时提示预约要求或预约入口。
- 对票价、预约、交通可达性等信息，系统需要标明来源、更新时间或置信度。
- 每次打开行程时，系统需要重新查询票务状态。
- 票务查询结果需要展示提示语：“查询结果仅供参考，请以购票平台为准”。
- 票务查询结果需要列出查询时间和信息来源。
- 票务来源需要按可信度排序，官方平台优先、聚合平台其次、搜索结果最后。
- 公开搜索结果需要展示来源链接；当存在多个来源链接时默认折叠，用户点击后展开。
- 不同搜索结果后面需要展示对应的信息来源。
- 行程规划需要纳入天气、人数和交通拥挤程度提示。
- 旅游当天需要通过邮件提醒天气风险。
- Demo 阶段邮件提醒只生成邮件内容和发送状态模拟，不真实发送。
- 邮箱在创建提醒时临时填写，并绑定当次灵感或当次行程。
- 偏好摘要卡片展示在左侧 Agent 对话区。
- 后续多人协作阶段，用户可以通过分享链接邀请他人查看、评论或投票，Agent 可以基于多人评论和投票结果生成或调整折中方案。

## 10. 明确暂不默认纳入的范围

以下能力用户尚未明确要求，暂不作为已确认需求：

- 直接下单、支付、出票。
- 出境游完整适配。
- 酒店预订闭环。
- 餐厅预订闭环。
- 生产级账户体系和跨设备同步。
- 移动端浏览器专项适配。
- 第一阶段真实多人在线协作。
- 生产级多人权限管理、消息通知和审计。
- 行程导出 PDF、图片、日历或 Markdown。
- 固定高质量样例数据作为主要演示路径。
- 第一版注册/登录体系。
- 第一版复杂字段类型配置。
- 默认长期保存用户上传原图。
- 用户自定义坏天气阈值配置。
- 预算硬约束模式。
- 第一版真实发送邮件。
- 第一版拆分多个业务页面。
- 社区内容发布。
- 旅行结束后的游记生成。
- 商家广告投放。
- 企业差旅审批。

## 11. 待澄清问题

为进入下一版 PRD，需要优先确认以下问题：

1. Spec Kit 下一版是否需要把推荐技术栈和阶段路线图写回本需求文档，还是只保留在 specs/001-ai-travel-planner/plan.md？
2. 默认 provider 的具体服务商是否在实现前再定，还是现在就先做推荐选型？
3. 第一版主页面是否需要保存多个历史行程入口，还是只保存当前单个行程？
